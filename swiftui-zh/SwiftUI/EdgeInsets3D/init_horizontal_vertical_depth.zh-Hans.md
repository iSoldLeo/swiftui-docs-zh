---
来源：https://developer.apple.com/documentation/SwiftUI/EdgeInsets3D/init(水平:垂直:深度:)
抓取时间：2025-12-03T06:39:22Z
---

# init(horizontal:vertical:depth:)

**Initializer**

创建`EdgeInsets3D` 值，并为每个轴提供值。

### 声明

```swift
init(horizontal: CGFloat = 0, vertical: CGFloat = 0, depth: CGFloat = 0)
```

## 参数

- **horizontal**：沿水平轴应用的嵌入。
- **vertical**：沿垂直轴应用的嵌入。
- **depth**：沿深度轴应用的嵌入。

## 创建边缘嵌入

- **init(top:leading:bottom:trailing:front:back:)**：创建`EdgeInsets3D`值，并为每个面提供数值。


---
source: https://developer.apple.com/documentation/SwiftUI/EdgeInsets3D/init(horizontal:vertical:depth:)
crawled: 2025-12-03T06:39:22Z
---

# init(horizontal:vertical:depth:)

**Initializer**

Creates an `EdgeInsets3D` value with values provided for each axis.

## Declaration

```swift
init(horizontal: CGFloat = 0, vertical: CGFloat = 0, depth: CGFloat = 0)
```

## Parameters

- **horizontal**: The insets to apply along the horizontal axis.
- **vertical**: The insets to apply along the vertical axis.
- **depth**: The insets to apply along the depth axis.

## Creating an edge inset

- **init(top:leading:bottom:trailing:front:back:)**: Creates an `EdgeInsets3D` value with values provided for each face.

