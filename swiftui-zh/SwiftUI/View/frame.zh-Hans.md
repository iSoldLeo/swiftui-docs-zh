--- 来源：https://developer.apple.com/documentation/SwiftUI/View/frame()
抓取时间：2025-12-01T10:24:49Z

---

# frame()

**实例方法**

将此视图定位在一个不可见的框架内。

## 声明

```swift
nonisolated func frame() -> some View

```

## 布局修饰符

- **edgesIgnoringSafeArea(_:)**：更改视图的建议区域，使其超出屏幕的安全区域。

- **coordinateSpace(name:)**：为视图的坐标空间指定一个名称，以便其他代码可以相对于该命名空间操作点和大小等尺寸。


---
source: https://developer.apple.com/documentation/SwiftUI/View/frame()
crawled: 2025-12-01T10:24:49Z
---

# frame()

**Instance Method**

Positions this view within an invisible frame.

## Declaration

```swift
nonisolated func frame() -> some View

```

## Layout modifiers

- **edgesIgnoringSafeArea(_:)**: Changes the view’s proposed area to extend outside the screen’s safe areas.
- **coordinateSpace(name:)**: Assigns a name to the view’s coordinate space, so other code can operate on dimensions like points and sizes relative to the named space.

